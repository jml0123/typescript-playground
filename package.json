{
  "name": "simple-typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -w",
    "dev": "npx concurrently -k -n COMPILER,NODEMON -c yellow,green \"tsc -w\" \"nodemon -w dist -q dist/index.js\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4",
    "typescript": "^3.9.7"
  }
}
